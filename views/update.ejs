<!-- <%- include("./partials/header.ejs") %>

<div class="container">
    <div class="row">
        <div class="col-md-8 mx-auto">
            <div class="card">
                <div class="card-body">
                    <h1>Modifier un article:<%= title %></h1>
                    <form action="/articles/<%= article.id %>/update" method="post">
                        <div class="mb-3">
                          <label for="title" class="form-label">Titre</label>
                            <input type="text" class="form-control" id="title"  name="title" placeholder="titre de votre article" value="<%= article.title">
                        </div>
                        <div class="mb-3">
                          <label for="author" class="form-label">Auteur de l'article</label>
                            <input type="text" class="form-control" id="author"  name="author"  placeholder="Auteur de l'article" value="<%= article.author">>
                          </div>
                        <div class="mb-3">
                          <label for="resume" class="form-label">Résume de l'article</label>
                            <textarea class="form-control" id="resume" name="resume"  rows="3">
                                <%= article.resume">
                            </textarea>
                          </div>
                        <div class="mb-3">
                          <label for="contenu" class="form-label">Contenu de l'article</label>
                            <textarea class="form-control" id="contenu" name="contenu"  rows="3"></textarea>
                        </div>
                          <button type="submit" class="btn btn-primary">Valider</button>  
                      </form>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include("./partials/footer.ejs") %> -->